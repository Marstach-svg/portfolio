# ポートフォリオサイト構築指示書

## 概要
モダンでおしゃれな個人ポートフォリオサイトを作成してください。
2026年のWebデザイントレンドを取り入れた、インタラクティブで印象的なサイトを目指します。

## 技術スタック（必須）
- **フレームワーク**: Next.js 15 (App Router, TypeScript)
- **スタイリング**: Tailwind CSS v4
- **アニメーション**: Framer Motion (ページ遷移、スクロールアニメーション)
- **3D（任意）**: React Three Fiber（ヒーローセクションに簡単な3Dオブジェクトを入れる場合）
- **アイコン**: Lucide React
- **フォント**: Google Fonts から Geist または Inter + Noto Sans JP
- **デプロイ**: Vercel 前提の設定
- **データベース**: 不使用（Markdown / MDX でコンテンツ管理）
- **パッケージマネージャー**: pnpm

## デザイン要件

### 全体方針
- ダークモードベース（背景: #0a0a0a 〜 #111111 系）
- アクセントカラー: ブルー系グラデーション（#3b82f6 → #8b5cf6）
- フォントサイズは大きめ、余白は多めのミニマルデザイン
- グレインノイズやグラデーションのオーバーレイで質感を出す
- レスポンシブ対応必須（モバイルファースト）

### アニメーション
- ページ読み込み時にヒーローセクションの要素がスタガーでフェードイン
- スクロールに応じて各セクションの要素がフェードイン（Framer Motion の whileInView）
- ページ遷移はスムーズなフェードトランジション
- ナビゲーションにホバーアニメーション
- カスタムカーソル（オプション：PCのみ、小さな円形カーソル）

## ページ構成

### 1. ヒーローセクション (/)
- フルスクリーン
- 名前を大きなタイポグラフィで表示
- 肩書 / キャッチコピーを1行で
- 下スクロールを促すアニメーションアイコン
- 背景: グラデーション + 微細なグリッドパターンまたはパーティクル

### 2. About セクション
- プロフィール写真（丸型 or 角丸）
- 自己紹介テキスト（2〜3段落）
- 経歴ハイライトをタイムライン形式で表示

### 3. Skills セクション
- Bento Grid レイアウト（Apple風のカード配置）
- 各カードにアイコン + 技術名 + 習熟度バーまたはラベル
- カテゴリ分け: Frontend / Backend / Tools / Other
- ホバーでカードが微妙にリフトアップするエフェクト

### 4. Projects セクション
- プロジェクトカードのグリッド表示（2列 or 3列）
- 各カードに: サムネイル画像、タイトル、短い説明、使用技術タグ
- カードクリックでモーダルまたは詳細ページに遷移
- フィルタリング機能（カテゴリ別: Web App / Mobile / OSS など）
- GitHubリンク & ライブデモリンクをボタンで配置

### 5. Experience セクション
- 縦のタイムライン形式
- 会社名/プロジェクト名、期間、役割、概要を表示
- スクロールに応じてタイムラインが伸びるアニメーション

### 6. Blog セクション（任意）
- MDX で記事管理
- 記事カードのリスト表示（最新3件）
- 「すべての記事を見る」リンク
- 記事ページ: 目次、コードハイライト（Shiki）、推定読了時間

### 7. Contact セクション
- メールアドレスを大きく表示 or コピーボタン
- SNSリンク（GitHub, X/Twitter, LinkedIn, Zenn 等）をアイコンで
- 簡易コンタクトフォーム（Resend or EmailJS で送信）

### 8. フッター
- コピーライト
- 「Built with Next.js」等の技術バッジ
- トップに戻るボタン

## ファイル構成（参考）
src/
├── app/
│   ├── layout.tsx         # ルートレイアウト（フォント、メタデータ）
│   ├── page.tsx           # ホームページ（全セクション or セクションコンポーネント呼び出し）
│   ├── blog/
│   │   ├── page.tsx       # ブログ一覧
│   │   └── [slug]/
│   │       └── page.tsx   # ブログ記事詳細
│   └── globals.css        # グローバルスタイル
├── components/
│   ├── layout/
│   │   ├── Header.tsx     # ナビゲーションバー
│   │   └── Footer.tsx
│   ├── sections/
│   │   ├── Hero.tsx
│   │   ├── About.tsx
│   │   ├── Skills.tsx
│   │   ├── Projects.tsx
│   │   ├── Experience.tsx
│   │   ├── Blog.tsx
│   │   └── Contact.tsx
│   └── ui/
│       ├── Card.tsx
│       ├── Button.tsx
│       ├── Badge.tsx
│       ├── Timeline.tsx
│       └── BentoGrid.tsx
├── content/
│   ├── projects.ts        # プロジェクトデータ
│   ├── experience.ts      # 経歴データ
│   ├── skills.ts          # スキルデータ
│   └── blog/              # MDXファイル置き場
├── lib/
│   ├── mdx.ts             # MDX処理ユーティリティ
│   └── utils.ts           # 汎用ユーティリティ
└── types/
└── index.ts           # 型定義

## SEO・パフォーマンス要件
- Next.js の Metadata API でメタタグを適切に設定
- OGP画像を自動生成（next/og）
- sitemap.xml, robots.txt を生成
- Lighthouse スコア: Performance 90+, Accessibility 95+, SEO 100
- 画像は next/image で最適化
- フォントは next/font で最適化読み込み

## 注意事項
- すべてのテキストはプレースホルダーで構いません（後で自分の情報に差し替えます）
- コンポーネントは再利用性を意識して分離してください
- アクセシビリティ（WCAG 2.1 AA）に準拠してください
- 日本語と英語の両方が表示可能な設計にしてください
- コメントは日本語で記載してください

## 補足：カスタマイズのヒント
さらに差別化するためのアイデア

ターミナル風ヒーロー: タイピングアニメーションでコマンドライン風に自己紹介を表示
ゲーミフィケーション: スキルをRPGのステータス画面風に表示、訪問者がミニゲームをプレイできる仕掛け
AI チャットボット: 自分に関する質問に答えるチャットUIを設置（OpenAI API等で実装）
GitHub Contributions連携: GitHub APIからコントリビューションヒートマップをリアルタイム表示
Spotify連携: 「今聴いている曲」をリアルタイム表示して親しみやすさを演出